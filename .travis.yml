language: node_js
node_js:
- 7
- 8
- 10
before_install:
- npm install -g npm
script:
- npm install
- npm run pretest
deploy:
  provider: heroku
  api_key:
    secure: $TEST 
  app: mine-travis-test
  on:
    repo: giacomo-secchi/mine-travis-test

